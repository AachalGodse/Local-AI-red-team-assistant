# Core runtime deps (MVP — deliberately minimal)
typer>=0.12.0
rich>=13.0
pyyaml>=6.0
requests>=2.31
# Note: GhostOps talks to Ollama over its HTTP API via `requests`, so the
# `ollama` python client is NOT required. Reporting is plain-string Markdown,
# so `jinja2` isn't needed either. Both were dropped to keep deps minimal.
